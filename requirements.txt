# Python ovisnosti koje kod stvarno koristi (provjereno prema importima u
# src/ i scripts/). ROS 2 pakete instalira apt/rosdep, ne ovaj popis.
numpy           # čvorovi, mjerenja, obrada oblaka točaka
scipy           # transformacije, distance_transform_edt u nav_zones
opencv-python   # cv2.aruco - vlastiti detektor markera
Pillow          # generiranje ArUco tekstura (scripts/make_marker.py)
python-dotenv   # LiteRealm konfiguracija i okruženje

